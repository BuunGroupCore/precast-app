import { APP_CONFIG } from "@/config/constants";

/**
 * Hero section component showcasing the project
 * Clean, modern design without overwhelming effects
 */
export function Hero() {
  return (
    <div className="text-center mb-20">
      <div className="mb-8">
        <h1 className="text-6xl sm:text-7xl lg:text-8xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-pink-400 to-purple-600 mb-6">
          React + Tailwind
        </h1>
      </div>
      
      <p className="text-2xl md:text-3xl text-gray-300 mb-8 leading-relaxed max-w-4xl mx-auto">
        Modern React application with 
        <span className="text-cyan-400 font-semibold"> Tailwind CSS</span> and 
        <span className="text-blue-400 font-semibold"> TypeScript</span>
      </p>
      
      <div className="inline-flex items-center gap-2 px-6 py-3 bg-slate-800/50 rounded-full border border-slate-700/50 backdrop-blur-sm">
        <span className="text-gray-400">Project:</span>
        <span className="text-white font-semibold">{APP_CONFIG.displayName}</span>
        <div className="w-2 h-2 bg-blue-500 rounded-full animate-pulse" title="TypeScript"></div>
      </div>
    </div>
  );
}