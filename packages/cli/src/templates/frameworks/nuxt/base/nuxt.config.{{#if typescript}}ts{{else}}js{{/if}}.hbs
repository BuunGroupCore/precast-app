// https://nuxt.com/docs/api/configuration/nuxt-config
export default defineNuxtConfig({
  devtools: { enabled: true },
  {{#if typescript}}typescript: {
    typeCheck: true
  },{{/if}}
  {{#if (eq styling "tailwind")}}modules: [
    '@nuxtjs/tailwindcss'{{#ifAny (eq backend "express") (eq backend "fastify") (eq backend "hono")}},
    '@pinia/nuxt'{{/ifAny}}
  ],{{else if (eq styling "scss")}}css: [
    '@/assets/scss/main.scss'
  ],
  {{#ifAny (eq backend "express") (eq backend "fastify") (eq backend "hono")}}modules: [
    '@pinia/nuxt'
  ],{{/ifAny}}{{else}}{{#ifAny (eq backend "express") (eq backend "fastify") (eq backend "hono")}}modules: [
    '@pinia/nuxt'
  ],{{/ifAny}}{{/if}}
  app: {
    head: {
      title: '{{name}}',
      meta: [
        { name: 'description', content: 'Generated by {{name}}' }
      ]
    }
  }
})