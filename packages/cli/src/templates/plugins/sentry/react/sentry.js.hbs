/**
 * Sentry Integration Wrapper
 * @module sentry
 * @description Sentry error tracking integration for React applications
 */

import "./instrument";
import * as Sentry from "@sentry/react";

export const withSentry = Sentry.withSentryRouting;
export const ErrorBoundary = Sentry.ErrorBoundary;