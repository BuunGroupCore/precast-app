{{#if (eq styling "tailwind")}}
@tailwind base;
@tailwind components;
@tailwind utilities;
{{else}}
/* Global styles */
html {
  font-family: system-ui, sans-serif;
}

body {
  margin: 0;
  padding: 0;
  min-height: 100vh;
}

* {
  box-sizing: border-box;
}
{{/if}}