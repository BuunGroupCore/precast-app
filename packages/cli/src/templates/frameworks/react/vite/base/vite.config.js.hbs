import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src')
    }
  },
  server: {
    port: 3000{{#if (includes powerups "ngrok")}},
    host: '0.0.0.0', // Listen on all interfaces for Docker/ngrok access
    // Allow ngrok tunnels
    allowedHosts: ["localhost", ".ngrok.app", ".ngrok-free.app", ".ngrok.io"]{{/if}}
  }
})