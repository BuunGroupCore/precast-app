import { Hero, GettingStarted, TechnologyStack, CTA } from "@/components/home";

/**
 * Home page component
 * Main landing page showcasing the generated stack
 */
export function HomePage() {
  return (
    <div className="{{#if (eq styling 'tailwind')}}max-w-6xl mx-auto px-4 py-16 sm:px-6 sm:py-24 lg:px-8{{else}}page-container{{/if}}">
      <Hero />
      <GettingStarted />
      <TechnologyStack />
      <CTA />
    </div>
  );
}