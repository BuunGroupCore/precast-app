import type { MetaFunction } from "@remix-run/node";
import Counter from "~/components/Counter";
import HelloWorld from "~/components/HelloWorld";

export const meta: MetaFunction = () => {
  return [
    { title: "{{name}}" },
    { name: "description", content: "Welcome to {{name}}!" },
  ];
};

export default function Index() {
  return (
    <div className="{{#if (eq styling 'tailwind')}}min-h-screen flex flex-col items-center justify-center p-8 bg-gradient-to-br from-blue-50 to-indigo-100{{else}}container{{/if}}">
      <main className="{{#if (eq styling 'tailwind')}}text-center space-y-8{{else}}main{{/if}}">
        <h1 className="{{#if (eq styling 'tailwind')}}text-4xl font-bold text-gray-800 mb-8{{else}}title{{/if}}">
          Welcome to {{name}}
        </h1>
        
        <div className="{{#if (eq styling 'tailwind')}}space-y-6{{else}}components{{/if}}">
          <HelloWorld />
          <Counter />
        </div>
        
        <div className="{{#if (eq styling 'tailwind')}}text-gray-600{{else}}links{{/if}}">
          <p>
            Get started by editing{' '}
            <code className="{{#if (eq styling 'tailwind')}}bg-gray-100 px-2 py-1 rounded text-sm font-mono{{else}}code{{/if}}">
              app/routes/_index.tsx
            </code>
          </p>
        </div>
      </main>
    </div>
  );
}