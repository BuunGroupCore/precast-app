<template>
  <div{{#if (eq styling "tailwind")}} class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8"{{/if}}>
    <div{{#if (eq styling "tailwind")}} class="max-w-7xl mx-auto"{{else}} class="container"{{/if}}>
      <header{{#if (eq styling "tailwind")}} class="text-center mb-12"{{/if}}>
        <h1{{#if (eq styling "tailwind")}} class="text-4xl font-bold text-gray-900 mb-4"{{else}} class="title"{{/if}}>Welcome to {{name}}</h1>
        <p{{#if (eq styling "tailwind")}} class="text-lg text-gray-600"{{else}} class="subtitle"{{/if}}>A Vue.js project with {{styling}}{{#if typescript}} and TypeScript{{/if}}</p>
      </header>

      <main{{#if (eq styling "tailwind")}} class="grid grid-cols-1 md:grid-cols-2 gap-8"{{/if}}>
        <Counter />
        <HelloWorld msg="Hello from Vue!" />
      </main>
    </div>
  </div>
</template>

<script{{#if typescript}} lang="ts"{{/if}} setup>
import HelloWorld from './components/HelloWorld.vue'
import Counter from './components/Counter.vue'
</script>

{{#unless (eq styling "tailwind")}}
<style{{#if (eq styling "scss")}} lang="scss"{{/if}} scoped>
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem;
}

.title {
  font-size: 2.5rem;
  margin-bottom: 1rem;
  text-align: center;
}

.subtitle {
  font-size: 1.2rem;
  color: #666;
  text-align: center;
  margin-bottom: 3rem;
}

main {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}
</style>
{{/unless}}