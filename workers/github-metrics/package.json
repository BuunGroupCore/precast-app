{
  "name": "github-metrics-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to fetch GitHub metrics and cache to R2",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --test-scheduled"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.30.0"
  }
}
