import { Github, ExternalLink, Terminal } from "lucide-react";

/**
 * Footer component - Brutalist terminal-style footer
 */
export function Footer() {
  return (
    <footer className="{{#if (eq styling 'tailwind')}}mt-auto border-t-4 border-black bg-black{{else}}footer{{/if}}">
      <div className="{{#if (eq styling 'tailwind')}}max-w-6xl mx-auto px-6 py-6{{else}}footer-content{{/if}}">
        <div className="{{#if (eq styling 'tailwind')}}flex flex-col md:flex-row items-center justify-between gap-6{{else}}footer-inner{{/if}}">
          {/* Terminal prompt */}
          <div className="{{#if (eq styling 'tailwind')}}flex items-center space-x-3 font-mono text-sm{{else}}footer-prompt{{/if}}">
            <Terminal size={16} className="{{#if (eq styling 'tailwind')}}text-yellow-400{{else}}icon-primary{{/if}}" />
            <span className="{{#if (eq styling 'tailwind')}}text-yellow-400{{else}}prompt-symbol{{/if}}">$</span>
            <span className="{{#if (eq styling 'tailwind')}}text-white{{else}}prompt-text{{/if}}">{{projectName}}</span>
            <span className="{{#if (eq styling 'tailwind')}}text-yellow-400 animate-pulse{{else}}prompt-cursor{{/if}}">_</span>
          </div>

          {/* Links */}
          <div className="{{#if (eq styling 'tailwind')}}flex items-center gap-6{{else}}footer-links{{/if}}">
            <a
              href="https://precast.dev"
              target="_blank"
              rel="noopener noreferrer"
              className="{{#if (eq styling 'tailwind')}}group flex items-center gap-2 text-yellow-400 hover:text-white transition-colors font-mono text-xs uppercase tracking-wider{{else}}footer-link{{/if}}"
            >
              <span>[PRECAST.DEV]</span>
              <ExternalLink size={12} className="{{#if (eq styling 'tailwind')}}group-hover:translate-x-1 transition-transform{{else}}link-icon{{/if}}" />
            </a>
            <a
              href="https://github.com/BuunGroupCore/precast-app"
              target="_blank"
              rel="noopener noreferrer"
              className="{{#if (eq styling 'tailwind')}}group flex items-center gap-2 text-yellow-400 hover:text-white transition-colors font-mono text-xs uppercase tracking-wider{{else}}footer-link{{/if}}"
            >
              <Github size={14} />
              <span>[SOURCE]</span>
            </a>
          </div>
        </div>

        {/* Status bar */}
        <div className="{{#if (eq styling 'tailwind')}}mt-4 pt-4 border-t border-yellow-400/20 flex flex-wrap items-center justify-between text-xs font-mono{{else}}status-bar{{/if}}">
          <div className="{{#if (eq styling 'tailwind')}}flex items-center gap-4 text-gray-500{{else}}status-left{{/if}}">
            <span>STATUS: <span className="{{#if (eq styling 'tailwind')}}text-yellow-400{{else}}status-value{{/if}}">ONLINE</span></span>
            <span>MODE: <span className="{{#if (eq styling 'tailwind')}}text-yellow-400{{else}}status-value{{/if}}">DEV</span></span>
            <span>VERSION: <span className="{{#if (eq styling 'tailwind')}}text-yellow-400{{else}}status-value{{/if}}">1.0.0</span></span>
          </div>
          <div className="{{#if (eq styling 'tailwind')}}text-gray-500{{else}}status-right{{/if}}">
            GENERATED BY <span className="{{#if (eq styling 'tailwind')}}text-yellow-400{{else}}status-value{{/if}}">PRECAST CLI</span>
          </div>
        </div>
      </div>
    </footer>
  );
}