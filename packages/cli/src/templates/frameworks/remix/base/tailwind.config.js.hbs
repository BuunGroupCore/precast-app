{{#if (eq styling "tailwind")}}/** @type {import('tailwindcss').Config} */
export default {
  content: ["./app/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [{{#if (eq uiLibrary "daisyui")}}
    require("daisyui"),{{/if}}
  ],{{#if (eq uiLibrary "daisyui")}}
  daisyui: {
    themes: ["light", "dark", "cupcake"],
  },{{/if}}
}{{/if}}