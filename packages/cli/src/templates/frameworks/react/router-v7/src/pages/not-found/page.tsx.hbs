import { Link } from "react-router";

/**
 * 404 Not Found page
 */
export function NotFoundPage() {
  return (
    <div className="{{#if (eq styling 'tailwind')}}flex flex-col items-center justify-center min-h-[60vh] text-center px-4{{else}}not-found-container{{/if}}">
      <h1 className="{{#if (eq styling 'tailwind')}}text-6xl font-bold text-gray-900 dark:text-white mb-4{{else}}not-found-title{{/if}}">404</h1>
      <h2 className="{{#if (eq styling 'tailwind')}}text-2xl font-semibold text-gray-700 dark:text-gray-300 mb-4{{else}}not-found-subtitle{{/if}}">Page Not Found</h2>
      <p className="{{#if (eq styling 'tailwind')}}text-gray-600 dark:text-gray-400 mb-8{{else}}not-found-text{{/if}}">
        The page you're looking for doesn't exist or has been moved.
      </p>
      <Link
        to="/"
        className="{{#if (eq styling 'tailwind')}}px-6 py-3 bg-blue-600 text-white rounded-lg font-semibold hover:bg-blue-700 transition-colors{{else}}btn btn-primary{{/if}}"
      >
        Go Back Home
      </Link>
    </div>
  );
}