import { ArrowRight, Zap, Code2, Palette, Shield, Rocket, Star, BookOpen, Sparkles } from "lucide-react";

/**
 * Feature item interface
 */
interface Feature {
  icon: React.ReactNode;
  title: string;
  description: string;
  highlight?: boolean;
}

/**
 * Call-to-Action component showcasing React app capabilities
 * Beautifully displays technology choices and guides next steps
 */
export function CTA() {
  const features: Feature[] = [
    {
      icon: <Zap className="w-7 h-7" />,
      title: "Lightning Fast Development",
      description: "Hot reload, instant feedback, and optimized build tools for the best developer experience"
    },
    {
      icon: <Code2 className="w-7 h-7" />,
      title: "React Application",
      description: "Modern React frontend application built with TypeScript for type safety and better developer experience",
      highlight: true
    },
    {
      icon: <Palette className="w-7 h-7" />,
      title: "Tailwind CSS Styling",
      description: "Utility-first CSS framework for rapid, consistent UI development with beautiful design systems"
    },
    {
      icon: <Shield className="w-7 h-7" />,
      title: "Node.js Runtime",
      description: "Mature, stable JavaScript runtime with extensive ecosystem and enterprise support"
    },
    {
      icon: <Sparkles className="w-7 h-7" />,
      title: "Developer Experience",
      description: "Hot reload, instant feedback, optimized build tools, and modern development workflows for maximum productivity"
    }
  ];

  return (
    <section className="relative mt-16">
      {/* Background gradient */}
      <div className="absolute inset-0 bg-gradient-to-br from-purple-900/20 via-transparent to-pink-900/20 rounded-3xl"></div>
      
      <div className="relative bg-gradient-to-br from-slate-800/80 to-slate-900/80 backdrop-blur-xl rounded-3xl p-8 md:p-12 shadow-2xl border border-slate-700/50">
        <div className="text-center mb-16">
          <div className="flex items-center justify-center gap-3 mb-6">
            <Rocket className="w-10 h-10 text-purple-400" />
            <h2 className="text-4xl md:text-5xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-400">
              Your Stack is Ready
            </h2>
          </div>
          
          <p className="text-xl md:text-2xl text-gray-300 max-w-3xl mx-auto leading-relaxed">
            Everything you need to build modern web applications. 
            <span className="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-400 font-semibold"> Precast </span>
            has configured your perfect development environment.
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8 mb-16">
          {features.map((feature, index) => (
            <div 
              key={index} 
              className={`relative group ${feature.highlight ? 'md:col-span-2 xl:col-span-1' : ''}`}
            >
              {feature.highlight && (
                <div className="absolute -inset-1 bg-gradient-to-r from-purple-600 to-pink-600 rounded-3xl blur opacity-20 group-hover:opacity-30 transition duration-500"></div>
              )}
              <div className={`relative h-full bg-slate-700/40 backdrop-blur-sm rounded-2xl p-8 border transition-all duration-300 group-hover:scale-[1.02] ${
                feature.highlight 
                  ? 'border-purple-500/50 group-hover:border-purple-400/70' 
                  : 'border-slate-600/50 group-hover:border-slate-500/70'
              }`}>
                <div className="flex flex-col h-full">
                  <div className={`inline-flex items-center justify-center w-16 h-16 rounded-2xl mb-6 ${
                    feature.highlight 
                      ? 'bg-gradient-to-br from-purple-600/30 to-pink-600/30 text-purple-300' 
                      : 'bg-slate-600/30 text-slate-300 group-hover:text-purple-400'
                  } transition-colors duration-300`}>
                    {feature.icon}
                  </div>
                  
                  <h3 className="text-xl font-bold text-white mb-4 group-hover:text-purple-100 transition-colors">
                    {feature.title}
                  </h3>
                  
                  <p className="text-gray-400 leading-relaxed flex-grow">
                    {feature.description}
                  </p>
                </div>
              </div>
            </div>
          ))}
        </div>

        <div className="text-center">
          <div className="flex flex-col sm:flex-row gap-6 justify-center items-center mb-8">
            <a 
              href="https://precast.dev/docs" 
              target="_blank" 
              rel="noopener noreferrer"
              className="group inline-flex items-center gap-3 px-10 py-5 bg-gradient-to-r from-purple-600 to-pink-600 text-white font-semibold rounded-2xl hover:from-purple-700 hover:to-pink-700 transform transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:shadow-purple-500/25"
            >
              <BookOpen className="w-6 h-6" />
              <span className="text-lg">Explore Documentation</span>
              <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
            </a>
            
            <a 
              href="https://github.com/BuunGroupCore/precast-app" 
              target="_blank" 
              rel="noopener noreferrer"
              className="group inline-flex items-center gap-3 px-10 py-5 bg-slate-800/50 text-white font-semibold rounded-2xl hover:bg-slate-700/50 backdrop-blur-sm transition-all duration-300 border border-slate-600/50 hover:border-slate-500/70 hover:scale-105"
            >
              <Star className="w-6 h-6" />
              <span className="text-lg">Star on GitHub</span>
            </a>
          </div>
          
          <div className="flex items-center justify-center gap-2 text-gray-500">
            <span>Crafted with</span>
            <Sparkles className="w-4 h-4 text-purple-400 animate-pulse" />
            <span>by</span>
            <strong className="text-transparent bg-clip-text bg-gradient-to-r from-purple-400 to-pink-400">
              precast.dev
            </strong>
          </div>
        </div>
      </div>
    </section>
  );
}