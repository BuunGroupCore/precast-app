{
  "name": "{{name}}",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "{{#if typescript}}astro check && {{/if}}astro build",
    "preview": "astro preview",
    "astro": "astro"{{#if typescript}},
    "type-check": "astro check"{{/if}}{{#if (eq orm "prisma")}},
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"{{/if}}
  },
  "dependencies": {
    "astro": "^4.16.7"{{#ifAny (eq backend "express") (eq backend "fastify") (eq backend "hono")}},
    "axios": "^1.7.7"{{/ifAny}}{{#if (eq orm "prisma")}},
    "@prisma/client": "^5.20.0"{{/if}}
  },
  "devDependencies": {
    {{#if typescript}}"@astrojs/check": "^0.9.4",
    "typescript": "^5.7.2"{{#if (or (eq styling "tailwind") (eq styling "scss") (eq orm "prisma"))}},{{/if}}{{/if}}{{#if (eq styling "tailwind")}}
    "@astrojs/tailwind": "^5.1.2",
    "tailwindcss": "^3.4.13"{{#if (or (eq styling "scss") (eq orm "prisma"))}},{{/if}}{{/if}}{{#if (eq styling "scss")}}
    "sass": "^1.79.4"{{#if (eq orm "prisma")}},{{/if}}{{/if}}{{#if (eq orm "prisma")}}
    "prisma": "^5.20.0"{{/if}}
  }
}