{
  "name": "posthog-metrics-worker",
  "version": "1.0.0",
  "private": true,
  "description": "PostHog analytics worker for Precast app",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --env development",
    "deploy": "wrangler deploy --env production",
    "tail": "wrangler tail --env production",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
