{
  "name": "{{name}}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "{{#if typescript}}tsc && {{/if}}vite build",
    "preview": "vite preview"{{#if typescript}},
    "type-check": "tsc --noEmit"{{/if}}{{#if (eq orm "prisma")}},
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"{{/if}}
  },
  "dependencies": {
    "solid-js": "^1.9.2"{{#ifAny (eq backend "express") (eq backend "fastify") (eq backend "hono")}},
    "axios": "^1.7.7"{{/ifAny}}{{#if (eq orm "prisma")}},
    "@prisma/client": "^5.20.0"{{/if}}
  },
  "devDependencies": {
    "vite": "^5.4.8",
    "vite-plugin-solid": "^2.10.2"{{#if typescript}},
    "typescript": "^5.6.3"{{/if}}{{#if (eq styling "tailwind")}},
    "tailwindcss": "^3.4.13",
    "postcss": "^8.4.47",
    "autoprefixer": "^10.4.20"{{/if}}{{#if (eq styling "scss")}},
    "sass": "^1.79.4"{{/if}}{{#if (eq orm "prisma")}},
    "prisma": "^5.20.0"{{/if}}
  }
}