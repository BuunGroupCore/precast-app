{{#if typescript}}import React from 'react';{{else}}import React from 'react';{{/if}}
import { Hero } from '@/components/Hero';
import { APP_CONFIG } from '@/config/constants';

{{#if typescript}}
/**
 * Main application component - Minimal and beautiful design
 * Features the Precast banner and a clean footer
 * 
 * @component
 * @returns {JSX.Element} The rendered application
 */
const App: React.FC = () => {
{{else}}
/**
 * Main application component
 */
function App() {
{{/if}}
  return (
    {{#if (eq styling "tailwind")}}
    <div className="min-h-screen bg-gradient-to-b from-slate-900 via-purple-900/10 to-slate-900 flex flex-col">
      {/* Main content */}
      <main className="flex-grow">
        {/* Hero Section with Precast Banner */}
        <Hero />
      </main>

      {/* Minimal Footer */}
      <footer className="relative z-10 border-t border-white/10 bg-slate-900/50 backdrop-blur-xl">
        <div className="max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
          <div className="text-center">
            <p className="text-sm text-gray-400">
              Built with <span className="text-pink-400">♥</span> using{' '}
              <a 
                href="https://precast.app" 
                target="_blank" 
                rel="noopener noreferrer"
                className="text-purple-400 hover:text-purple-300 transition-colors"
              >
                Precast
              </a>
              {' '}• {new Date().getFullYear()} {APP_CONFIG.name}
            </p>
          </div>
        </div>
      </footer>
    </div>
    {{else if (eq styling "styled-components")}}
    <AppContainer>
      <MainContent>
        <Hero />
      </MainContent>
      <Footer>
        <FooterContent>
          <FooterText>
            Built with <Heart>♥</Heart> using{' '}
            <FooterLink href="https://precast.app" target="_blank" rel="noopener noreferrer">
              Precast
            </FooterLink>
            {' '}• {new Date().getFullYear()} {APP_CONFIG.name}
          </FooterText>
        </FooterContent>
      </Footer>
    </AppContainer>
    {{else}}
    <div className="app-container">
      <main className="main-content">
        <Hero />
      </main>
      <footer className="footer">
        <div className="footer-content">
          <p className="footer-text">
            Built with <span className="heart">♥</span> using{' '}
            <a 
              href="https://precast.app" 
              target="_blank" 
              rel="noopener noreferrer"
              className="footer-link"
            >
              Precast
            </a>
            {' '}• {new Date().getFullYear()} {APP_CONFIG.name}
          </p>
        </div>
      </footer>
    </div>
    {{/if}}
  );
}{{#if typescript}};{{else}}}{{/if}}

{{#if (eq styling "styled-components")}}
const AppContainer = styled.div`
  min-height: 100vh;
  background: linear-gradient(to bottom, #0f172a, #1e1b4b);
  display: flex;
  flex-direction: column;
`;

const MainContent = styled.main`
  flex-grow: 1;
`;

const Footer = styled.footer`
  position: relative;
  z-index: 10;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  background: rgba(15, 23, 42, 0.5);
  backdrop-filter: blur(12px);
`;

const FooterContent = styled.div`
  max-width: 1280px;
  margin: 0 auto;
  padding: 2rem 1rem;
  text-align: center;
`;

const FooterText = styled.p`
  font-size: 0.875rem;
  color: #9ca3af;
`;

const Heart = styled.span`
  color: #ec4899;
`;

const FooterLink = styled.a`
  color: #a855f7;
  text-decoration: none;
  transition: color 0.2s;
  
  &:hover {
    color: #9333ea;
  }
`;
{{/if}}

export default App;