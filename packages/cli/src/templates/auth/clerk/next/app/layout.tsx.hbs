import { ClerkProvider } from "@clerk/nextjs";
{{#if (eq styling "tailwind")}}
import "./globals.css";
{{else if (eq styling "scss")}}
import "./globals.scss";
{{/if}}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body>{children}</body>
      </html>
    </ClerkProvider>
  );
}